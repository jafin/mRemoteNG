## 1. Characterization tests (before any extraction)

- [x] 1.1 Add `mRemoteNGTests/Connection/Protocol/PuttyBaseCredentialArgumentTests.cs` pinning the argument string `PuttyBase.Connect()` produces for: plain password, no password, configured `PrivateKeyPath`, discovered default `.ppk`, and empty-username fallback (`windows` and `custom`). **Done 2026-08-08** — 17 tests, also covering domain qualification (3 cases), key precedence, and `Force.NoCredentials` suppression.
- [x] 1.2 ~~Extend coverage to each external provider with the provider interface faked.~~ **DESCOPED 2026-08-08 (maintainer decision: option c).** There is no provider interface to fake — all seven are static calls (`SecretServerInterface.FetchSecretFromServer`, `PasswordstateInterface.FetchSecretFromServer`, `OnePasswordCli.ReadPassword`, `PasswordSafeCli.ReadPassword`, `VaultOpenbao.ReadOtpSSH`/`ReadPasswordSSH`, `LAPSHelper.QueryLAPSPassword`). Rejected: adding ~7 `protected virtual` seams that task 4 would immediately delete. The cascade only produces three locals (username, password, privatekey) and 1.1 already pins how those become arguments, so **provider selection is covered at 3.7 against the real `ISshCredentialProvider` abstraction** rather than through PuTTY's command line. See design.md D8.
- [x] 1.3 Pin the PuTTY version split: `-pwfile` with a named pipe for >= 0.81, `-pw` below it. **Done 2026-08-08** — 3 tests including the exact-0.81 boundary; also asserts the password never appears in plaintext on the 0.81+ line.
- [x] 1.4 Run the targeted filter and confirm green. **Done 2026-08-08** — 17/17 in 1.4s; full suite 6589 passed / 0 failed. Note: "unmodified code" was not literally achievable — `Connect()` had no observable output, so four behaviour-preserving seams were added first (see 1.5).
- [x] 1.5 **Seam for testability (added, not originally planned).** `Connect()` built its arguments into a private `Process` and could not be observed. Added to `PuttyBase`: `internal string? LastBuiltArguments`, and `protected virtual` `DetectIsPuttyNg()`, `GetPuttyVersion()`, `CreatePasswordPipeArgument(string)`, `FindDefaultPrivateKey()`. All four are mechanical hoists of existing expressions — no logic change. Follows the existing `TestablePuttyBase` pattern in `PuttyBaseTests`. Deliberately **not** extracting argument construction; that is task 4.2's job, done under the protection of these tests.

## 2. Neutral credential model

- [x] 2.1 Add `mRemoteNG/Security/Ssh/ResolvedSshCredential.cs`: effective username, optional secret, optional key material, agent identities, provider provenance. Implements `IDisposable`. **Done 2026-08-08.** Provenance reuses the existing `ExternalCredentialProvider` enum rather than introducing a parallel one. Also added `SshAgentIdentity.cs` (backend-neutral, no SSH.NET or agent-library types) and `SshCredentialResolutionOptions.cs` (carries `DefaultKeyDiscoveryMode`, required because 3.4 needs PuTTY `.ppk` vs OpenSSH-format discovery to differ).
- [x] 2.2 Zero secret and key-material buffers on `Dispose`; add a test asserting the buffer is cleared. **Done 2026-08-08** — 20 tests in `mRemoteNGTests/Security/Ssh/ResolvedSshCredentialTests.cs`, including the zeroing contract, disposal idempotence, use-after-dispose, and that `ToString()` leaks neither secret nor key material. Buffers exposed `internal` (via existing `InternalsVisibleTo`) so zeroing is observable without reflection.
- [x] 2.3 Add `mRemoteNG/Security/Ssh/ISshCredentialResolver.cs`. **Done 2026-08-08.**
- [x] 2.4 Decide and document the single-shot vs re-invocable contract. **Done 2026-08-08 — re-invocable, never cached.** Recorded in design.md D7 and on the interface's XML doc. Rationale: Vault SSH-OTP is single-use, so caching breaks the second concurrent connection and every retry. Accepted cost: replayable providers are called once per attempt rather than once per connection.

## 3. Resolver implementation

- [x] 3.0 **`ISshCredentialProvider` abstraction (added, not originally planned).** 3.7/D8 assume it but 3.1 said "move verbatim", which would have kept the seven static calls inline and left 3.7 untestable for the same reason 1.2 was. Added `Providers/ISshCredentialProvider.cs` (interface + `SshProviderRequest` + `SshProviderResult`) and `Providers/ExternalConnectorProviders.cs` — six thin wrappers reproducing each provider's current exception handling and message text exactly, inconsistencies included (Delinea/Passwordstate/Vault raise via `ErrorOccured`; 1Password/PasswordSafe/LAPS write to the message collector; Vault's failure stays mislabelled "Secret Server Interface Error"). Also added `SshCredentialDiagnostic.cs` so the resolver can record *which channel* a message belongs on — it cannot call the protected `Event_ErrorOccured` itself.
- [x] 3.1 Add `mRemoteNG/Security/Ssh/SshCredentialResolver.cs`; move the provider cascade with no behaviour change. **Done 2026-08-08.** Two subtleties preserved deliberately, both pinned by tests: (a) `out` parameters mean a *failed* provider leaves the configured username/password intact while a *successful* one overwrites unconditionally even with empty values — hence `SshProviderResult.Succeeded`; (b) key material from 1Password/PasswordSafe is silently discarded today because the argument builder keys off the temp-file path only Delinea/Passwordstate write. (b) is a live bug, pinned rather than fixed.
- [x] 3.2 Move empty-credential fallback into the resolver. **Done 2026-08-08.** Note Vault and LAPS do not participate in the `custom` default-provider fallback today — only the four API-key providers do. Pinned.
- [x] 3.3 Move domain qualification into the resolver; return the effective username. **Done 2026-08-08** — `QualifyWithDomain`, applied to provider-supplied usernames too.
- [x] 3.4 Move default-key discovery, keeping `.ppk` for PuTTY and OpenSSH-format for OpenSSH. **Done 2026-08-08** — `IDefaultSshKeyLocator` + `DefaultSshKeyLocator`, selected per call via `SshCredentialResolutionOptions.DefaultKeyDiscovery`. The OpenSSH list includes `id_dsa` and the PuTTY list does not; that asymmetry is inherited, not introduced.
- [x] 3.5 Per-provider failure handling: record an error naming the provider, return the remaining credentials, do not throw. **Done 2026-08-08.**
- [x] 3.6 Provenance logging emits the provider name only. **Done 2026-08-08** — asserted by `ToStringNeverLeaksTheSecretOrKeyMaterial` (task 2.2 suite).
- [x] 3.7 Unit tests for the resolver covering every scenario in `specs/ssh-credential-resolution/spec.md`. **Done 2026-08-08** — 37 tests in `mRemoteNGTests/Security/Ssh/SshCredentialResolverTests.cs`. Not covered by unit test: the positive branch of the stored-default-password path, which needs `Runtime.EncryptionKey`; its three guard conditions are pinned instead, and the positive path is on the 11.4 smoke list. **Also absorbs descoped 1.2**: one test per external provider (Delinea, Passwordstate, 1Password, PasswordSafe, Vault password engine, Vault SSH-OTP, LAPS) asserting the selected provider, the resulting secret/key material, provenance, and the effective username after domain qualification — faked at `ISshCredentialProvider`, not at PuTTY's command line.

## 4. PuTTY adapter and cutover

- [x] 4.1 Add `mRemoteNG/Security/Ssh/Adapters/PuttyArgsAdapter.cs` emitting the existing credential arguments. **Done 2026-08-08.** Deliberately two entry points, not one: `AppendLoginAndPassword` sits inside the `Force.NoCredentials` guard while `AppendIdentity` sits outside it and after the Vault SSH-OTP block. Merging them would silently change which arguments `NoCredentials` suppresses. PuTTY version and the password-pipe factory are injected so the protocol keeps its overridable hooks.
- [x] 4.2 Switch `PuttyBase.Connect()` to resolver + adapter; delete the inline cascade. **Done 2026-08-08** — `PuttyBase.cs` net **-97 lines** (123 insertions, 220 deletions); zero remaining references to `ExternalConnectors.*`, `OptionsCredentialsPage` or `LegacyRijndael` in the file. Two things surfaced during the cutover: (a) added `ResolvedSshCredential.UnqualifiedUsername`, because the Vault SSH-OTP plugin is handed the *unqualified* name and matches it against its data request while `-l` on the same command line carries the qualified form — collapsing them would break SSH-OTP for any connection with a domain set; (b) discovery is routed back through the protocol's own `FindDefaultPrivateKey()` via `DelegateSshKeyLocator`, so the seam the characterization tests depend on stays live and the "auto-discovered SSH key" info message is still emitted.
- [x] 4.3 Re-run task 1 tests unmodified. **Done 2026-08-08 — 17/17 green with `git diff` on `mRemoteNGTests/` empty.** No test was edited to accommodate the refactor.
- [x] 4.4 Compile and run the `PuttyBase` and resolver filters. **Done 2026-08-08** — full build green (67.9s), full suite **6703/6703**.

## 5. Agent provider

- [x] 5.1 Add `SshNet.Agent` to `Directory.Packages.props` and `mRemoteNG/mRemoteNG.csproj`. **Done 2026-08-08** (maintainer-approved). Pinned as the exact range `[2024.2.0.5]` so it cannot float onto an unreviewed release, with a comment at the declaration saying why.
- [x] 5.2 Review the `SshNet.Agent` source before adoption; record the reviewed tag in `design.md`. **Done 2026-08-08 against tag `2024.2.0.5`**, recorded in design.md D4. Findings: untrusted agent input is bounded (`AgentReader` validates declared length against remaining stream, throws `EndOfStreamException` on short read); private keys never leave the agent; `SshAgentPrivateKey.Key` is `null` in exactly one place — the `sk-*` constructor — confirming the README caveat; one niche residual, the SSH-certificate constructors assign `Key = certificate.Key` unchecked.
- [x] 5.3 Add `mRemoteNG/Security/Ssh/Agent/ISshAgentProvider.cs` and `SshAgentIdentity`. **Done 2026-08-08.** `SshAgentIdentity` reduced to `(Comment, Algorithm)`: the speculative `PublicKeyBlob` was dropped because `SshAgentPrivateKey` exposes no wire-format blob and it would have been permanently empty. Task 9.1 will need a route from an identity back to its `IPrivateKeySource`; deferred until that requirement is concrete rather than guessed at now.
- [x] 5.4 Implement `SshNetAgentProvider` over `SshAgent` and `Pageant`. **Done 2026-08-08.** `Pageant` derives from `SshAgent`, so the library selects the named-pipe/`WM_COPYDATA` transport itself. Identities seen on both transports are de-duplicated, first-seen wins. Agent timeout set to 2s: it is paid per transport on every connection that consults the agent, so it is connection latency rather than background work.
- [x] 5.5 Filter `sk-*` identities; emit an informational message. **Done 2026-08-08** — one aggregated message per scan rather than one per key.
- [x] 5.6 Non-fatal failure handling. **Done 2026-08-08.** Writing the tests exposed a design weakness: the guarantee originally lived *inside* `ReadFrom`, so any override or future transport would silently lose it. Moved to `SafeReadFrom` at the contract boundary, materialising the sequence inside the guard so deferred enumeration cannot throw later.
- [x] 5.7 Unit tests covering every scenario in `specs/ssh-agent-authentication/spec.md`. **Done 2026-08-08** — 22 tests, none requiring a live agent. Includes `TheRealProviderLoadsAndReturnsWithoutAnAgentPresent`, the one test that exercises the real assembly: it took 5s at the old timeout, which confirms it genuinely reached `SshNet.Agent` and attempted a connection, so the `Renci.SshNet 2024.2.0.1` → `2025.1.0` roll-forward resolves at runtime.

## 6. Agent settings

- [x] 6.1 Add a global agent-authentication setting to `OptionsCredentialsPage.settings`, defaulting off until task 11.1. **Done 2026-08-08** — `UseSshAgent`, default `False`, added to both the `.settings` file and its `.Designer.cs`.
- [x] 6.2 ~~Add a per-connection setting on `ConnectionInfo` with an inherit-from-global default.~~ **DESCOPED 2026-08-08 (maintainer decision: option A, global only).** The `ssh-agent-authentication` spec was amended to require a single global setting and to state explicitly that no per-connection override exists, so the contract matches what is built. Not dropped for risk — the analysis below shows it *can* ship compatibly — but because the control has no counterpart in the tools mRemoteNG wraps (neither PuTTY nor `ssh.exe` has a per-connection agent toggle) and nothing has asked for per-host agent selection. Purely additive later at the same cost. Original measurement, retained because it applies unchanged whenever this is genuinely wanted: Measured against two comparable per-connection booleans (`UseCredSsp`, `RDPAlertIdleTimeout`): a new connection property touches ~19 files — `AbstractConnectionRecord`, `ConnectionInfo`, `ConnectionInfoInheritance`, `XmlConnectionNodeSerializer26/27/28`, `XmlConnectionsDeserializer`, CSV serializer + deserializer, SQL `DataTableSerializer`/`DataTableDeserializer`/`SqlDatabaseMetaDataRetriever`, `Language.Designer.cs`, `Settings.settings`, `ConnectionInfoPropertyGrid` — **and a new SQL schema version plus upgrader** (current max is 33). That ships a database migration and a connection-file format change inside a credential-resolver change. See design.md D10.
- [x] 6.3 Surface the setting in the credentials options page. **Done 2026-08-08** — `chkUseSshAgent` added to `CredentialsPage`, wired through `ApplyLanguage`/`LoadSettings`/`SaveSettings`, with a new `UseSshAgent` string in the neutral `Language.resx` + `Language.Designer.cs`. The 24 translated resx files fall back to English until translated, which is the normal path for a new string. Verified by the existing `AllPages` FrmOptions test, which loads every options page.
- [x] 6.4 Tests for setting resolution. **Done 2026-08-08** — 8 tests in `SshAgentSettingsTests`. Added `ISshAgentSettings`/`SshAgentSettings` (interface so callers are testable without the settings singleton) and `SshCredentialResolutionOptions.ForSshNet(bool)`, so the setting actually reaches resolution rather than only being stored. Asserts the shipped default is `False` by reading the declared `DefaultValue`, not the current user value, and pins that `ForPutty`/`ForOpenSsh` never consult the provider — the setting must not appear to disable native Pageant/Windows-agent support that mRemoteNG does not control.

## 7. Agent precedence and temp-file removal

- [x] 7.1 ~~Implement precedence in the resolver.~~ **Done 2026-08-08, but as ADDITIVE rather than precedence — D5 revised.** The resolver now takes an optional `ISshAgentProvider` and populates `ResolvedSshCredential.AgentIdentities` when `options.ConsultAgent` is set. Agent identities are offered *alongside* the configured key path, provider key material and discovered default — not instead of them. Reason: an agent holding *a* key says nothing about whether it holds *this connection's* key, so suppressing a vault key because an unrelated identity is loaded would break authentication intermittently. SSH auth offers candidate keys in turn and the server chooses; `PrivateKeyAuthenticationMethod` takes an array for exactly this reason. 6 new tests.
- [x] 7.2 ~~When the key source is an agent identity, skip the temp-file write.~~ **DEFERRED 2026-08-08 (maintainer decision: option C).** Filed as its own change, `add-ssh-agent-key-injection`, with the full analysis in its proposal. Deferred rather than dropped: the hazard is real but pre-existing, agent injection is a distinct mechanism with its own failure modes (key-format parsing, agent availability, key lifetime), and bundling it here would attach that risk to an otherwise well-tested refactor. The `ssh-agent-authentication` spec was amended to record the guarded temporary file as current behaviour and to name the deferral, so the contract matches what is built. Original analysis: Three independent reasons: (a) with additive semantics there is no "the key source is an agent identity" state to branch on; (b) the backend that writes the temp file is PuTTY, and PuTTY never consults `ISshAgentProvider` — `ForPutty` sets `ConsultAgent: false` because it talks to Pageant natively — so the condition could never fire; (c) the temp file is only written when a *vault* supplies a key, and a vault-supplied key is by definition not already in the agent. **The hazard is still removable by a different mechanism**: parse the vault key into an SSH.NET `PrivateKeyFile` and inject it into Pageant via `SshAgent.AddIdentity(key, lifetime, confirm)`, so it lives in agent memory with an expiry and never reaches disk. That is new work, not a skipped write — `PrivateKeyFile` parses OpenSSH/PEM but not `.ppk`, the vault's format is not guaranteed, Pageant must be running, and a temp-file fallback is still needed. See design.md D5.
- [x] 7.3 Test the temp-file behaviour. **Deferred with 7.2** to `add-ssh-agent-key-injection`. The behaviour that remains in this change — provider key material materialised to a temporary file, then zero-filled and deleted — is unchanged from before the refactor and is covered by the existing characterization tests (`ConfiguredPrivateKeyPathIsPassedWithIdentityFlag` and the resolver's `MaterialisedKeyMaterialSuppressesDiscovery`).
- [x] 7.4 ~~Confirm the PuTTY adapter emits no `-i` for provider key material when an agent identity is in play.~~ **Not applicable** — follows from 7.2(b): PuTTY never sees agent identities, so this state cannot arise. If 7.2 adopts agent injection, the adapter correctly emits no `-i` because there is no temp path to emit, which is already the existing behaviour rather than a new branch.

## 8. OpenSSH adapter

- [x] 8.1 Add `mRemoteNG/Security/Ssh/Adapters/OpenSshArgsAdapter.cs`. **Done 2026-08-08** — returns `OpenSshCredentialArguments(IdentityArgument, Destination, Unsupported)`. Also reports provider-supplied *key material* as unsupported, not just passwords: `ssh.exe` takes a key only as a file path, so vault-returned key bodies cannot be used either.
- [x] 8.2 Switch `ProtocolOpenSSH.BuildSshArguments()` to the resolver; keep OpenSSH-format default-key discovery. **Done 2026-08-08.** `ProtocolOpenSSH.FindDefaultSshKey` deleted — discovery now runs through `DefaultSshKeyLocator` in OpenSSH mode, preserving the `id_ed25519 / id_ecdsa / id_rsa / id_dsa` list. Added `protected virtual ResolveCredentials()` as a test seam.
- [x] 8.3 Report a resolved secret as unsupported. **Done 2026-08-08** — warning names the provider and states that `ssh.exe` has no option to accept a password non-interactively, and suggests a key or agent instead. The connection still proceeds; behaviour is unchanged, the diagnostic is what is new.
- [x] 8.4 Tests for the unsupported-password warning and the no-warning case. **Done 2026-08-08** — 14 tests in `OpenSshArgsAdapterTests`, including that the message never leaks the secret or key body, plus 2 resolver tests pinning the discovery gate below.
- [x] 8.5 Release-notes lines. **Two behaviour changes to announce**, both intended: (a) OpenSSH connections carrying vault credentials now emit a warning where they were previously silent — this exposes an already-broken configuration rather than creating one; (b) OpenSSH connections now apply the same empty-username fallback and domain qualification as PuTTY connections, so a connection with no username that previously let `ssh.exe` default to the local user may now send an explicit username. Required by the "applied once" requirement in `specs/ssh-credential-resolution/spec.md`.
- [x] 8.6 **Regression guard (found during 8.2, not originally planned).** PuTTY skips default-key discovery when a password is present; OpenSSH must not, because `ssh.exe` cannot use a password at all, so a stored password is not an alternative to a key. Reusing the PuTTY gate would have stripped the key an OpenSSH connection authenticates with today. Added `SshCredentialResolutionOptions.BackendAcceptsSecret` (true for PuTTY/SSH.NET, false for OpenSSH) to drive the gate, pinned by `DiscoveryStillRunsWithAPasswordWhenTheBackendCannotUseOne` and `DiscoveryIsStillSkippedWithAPasswordWhenTheBackendCanUseOne`.

## 9. SSH.NET adapter and dependency cleanup

- [x] 9.1 Add `mRemoteNG/Security/Ssh/Adapters/SshNetAuthAdapter.cs` producing `AuthenticationMethod[]`; emit agent identities as `PrivateKeyAuthenticationMethod` and always include `KeyboardInteractiveAuthenticationMethod`. **Done 2026-08-08.** Order is publickey → password → keyboard-interactive; all agent identities and key files go into one `PrivateKeyAuthenticationMethod`, matching what `ssh -i key` plus a loaded agent already does. Resolves the route deferred at 5.3: `SshAgentIdentity` gained an `internal object? KeyHandle` carrying the agent library's own key object. Weakly typed on purpose — typing it as `IPrivateKeySource` would put an SSH.NET type on the record the PuTTY and OpenSSH adapters also consume. The alternative, re-querying the agent when methods are built, costs a second round trip and lets the identities reported diverge from the ones offered. Also implements the D3 asymmetry concretely: SSH.NET *can* load provider-supplied key material from memory, so it is attempted here rather than reported unusable as it is for `ssh.exe`. `PasswordAuthenticationMethod`'s `byte[]` overload is used so the secret is never materialised as a `string`.
- [x] 9.2 Switch `SecureTransfer` to accept a `ResolvedSshCredential` instead of raw host/user/password. **Done 2026-08-08.** Ownership of the credential passes to `SecureTransfer`, which disposes it — the authentication methods read it lazily at authentication time, so it must outlive `Connect`. `Connect` was split: `CreateClient` does everything except the network I/O, which is the seam 9.5 tests through. `SSHTransferWindow` builds the credential from its own fields and, when the agent setting is on, consults the agent — see the release note under 9.6.
- [x] 9.3 Move the **SFTP branch** from APM `BeginUploadFile` / `SftpUploadAsyncResult` to native `UploadFileAsync`; delete the `Thread.Sleep(50)` progress poll in `SSHTransferWindow.StartTransferBG`. Leave the **SCP branch** synchronous — `ScpClient` exposes no async methods in SSH.NET 2025.1.0, so it keeps `Upload` + the `Uploading` event. The two branches stay deliberately asymmetric. **Done 2026-08-08.** `UploadFileAsync` takes no progress callback, so progress now comes from `ProgressReportingStream`, which counts bytes as the uploader reads them — finer-grained than the poll it replaces and updated only when something actually moves. Both protocols now report through one `SecureTransfer.UploadProgress` event, so the window no longer wires SSH.NET's `Uploading` event itself. The background thread still blocks (`GetAwaiter().GetResult()`): the thread exists to keep the upload off the UI thread, and making `StartTransferBG` `async void` would abandon its STA thread at the first await.
- [x] 9.4 Remove `Renci.SshNet.Async` from `Directory.Packages.props` and `mRemoteNG/mRemoteNG.csproj`. **Same sign-off as 5.1.** **Done 2026-08-08** (approved by maintainer). Verified: `project.assets.json` now has 0 references, `Renci.SshNet.Async.dll` gone from all three bin outputs, core `Renci.SshNet.dll` intact. Full build green (83.5s); full suite 6554/6555 with one pre-existing unrelated flake (see 9.7). Verified unreferenced — no source file imports it, yet it ships to `bin/x64/Release/Assemblies/` and both test outputs. **Independent of 9.1-9.3**; can land as a standalone cleanup at any point.
- [x] 9.5 Add `SecureTransfer` upload tests — there are none today; do not migrate the path uncovered. **Done 2026-08-08.** 35 new tests: `SshNetAuthAdapterTests` (17), `SecureTransferTests` (10), `ProgressReportingStreamTests` (8). No test requires a server or a running agent — `CreateClient` builds the client without I/O, so what a credential turns into is asserted directly on `ConnectionInfo.AuthenticationMethods`.
- [x] 9.6 Confirm `SSHTransferWindow` still functions end to end after the credential and async changes. **Deferred to 11.4** — needs a live SSH server, so it belongs with the manual smoke test rather than being claimed from a green unit-test run. **Done 2026-08-12 as item (e) of 11.4.** `SecureTransfer` connected to a real `sshd` with a password credential, uploaded 200,000 bytes over SFTP, and the bytes on the server matched. `ProgressReportingStream` reported the full count, so 9.3's replacement of the 50 ms poll works against a real transfer and not only against a fake stream. The window's own UI was not driven — what 9.2 and 9.3 changed is `SecureTransfer`, and that is what ran. **Since kept rather than left as a one-off:** `SecureTransferUploadTests` runs the same path against the existing `SftpServerFixture` on every suite run, so the refactor is protected by something that fails when it breaks instead of by a record of a smoke that once passed.

**Release notes from section 9:**
  - The SSH file transfer window can now authenticate with an SSH agent when the agent setting is enabled. Previously the typed password was the only credential it could offer, so a key-only host could not be reached from this window at all.
  - A keyboard-interactive question the server asks that mRemoteNG cannot answer — typically a second factor — is now named in the notifications panel instead of surfacing only as a generic authentication failure.
  - SFTP upload progress now updates as bytes are sent rather than on a 50 ms timer.

## 10. Spikes (can run in parallel from task 1)

- [x] 10.1 FIDO/`sk-*`: verify whether SSH.NET faults on `SshAgentPrivateKey.Key == null`. Resolves whether 5.5's filter can be lifted and whether task 8 default-on is safe. **Done 2026-08-08 — positive; filter lifted.** It cannot fault: `Key` is not a member of `IPrivateKeySource`, the entire surface SSH.NET consumes from a key source, so no code path in the authentication exchange can reach it. Separately, the pinned `SshNet.Agent 2024.2.0.5` carries the public-key blob and the agent's signature straight through for `sk-*` keys via `SkAgentHostAlgorithm` rather than needing a `Renci.SshNet.Security.Key` at all. Both facts are pinned by tests in `SshNetAuthAdapterTests`, so the filter goes back on automatically if either stops holding. `IncludeHardwareBacked` now defaults to `true`; excluding is still available per request. **Not verified against physical FIDO hardware** — none available. Residual risk is bounded: an `sk-*` identity that fails to sign costs one publickey attempt and falls through.
- [x] 10.2 `SSH_ASKPASS` / `SSH_ASKPASS_REQUIRE=force` on Win32-OpenSSH. If supported, 8.3's "unsupported" outcome could later become a supported path via an askpass helper. Record the finding in `design.md`; do not implement here. **Done 2026-08-08 — positive.** `OpenSSH_for_Windows_9.5p1, LibreSSL 3.8.2` contains `SSH_ASKPASS`, `SSH_ASKPASS_REQUIRE` and all three of its values (`prefer`/`force`/`never`). Exercised end to end through `read_passphrase()` — the same routine `ssh.exe`'s password prompt uses — by decrypting a passphrase-protected key with `ssh-keygen -y` under `SSH_ASKPASS_REQUIRE=force`, no tty, no `DISPLAY`; a plain `.bat` helper sufficed. The control run without the helper blocked on the console prompt. Recorded in `design.md` Open Questions along with why it is still not implemented: mRemoteNG would have to ship an askpass executable and get the secret to it without using a command line (visible in process listings) or an environment variable (readable by same-user processes) — a pipe, as `CreatePasswordPipeArgument` already does for PuTTY. Not implemented, per the task.
- [x] 10.3 ~~`ControlMaster` / `ControlPath` on Win32-OpenSSH.~~ **Done 2026-08-08 — negative.** `OpenSSH_for_Windows_9.5p1`: options parse and are echoed by `ssh -G` with no warning, but `ssh -O check` fails `getsockname failed: Not a socket`. Upstream `PowerShell/Win32-OpenSSH#1328` open since 2019-01-23, `0 - Backlog`. Recorded in `design.md` D3 and Open Questions. Consequence: the SFTP panel is SSH.NET-hosted; do **not** build any feature on `ssh.exe` multiplexing, and note that it fails silently rather than erroring.

## 11. Completion

- [x] 11.1 Resolve the sk-key spike; if clear, enable the agent setting by default and update `specs/ssh-agent-authentication/spec.md`. **Done 2026-08-08.** Spike cleared, so `UseSshAgent` now defaults to `True`. The spec's key-type requirement was rewritten to include `sk-*` and a new requirement was added covering large identity sets. That second requirement came out of a risk the spike surfaced: every identity costs one publickey attempt and OpenSSH's `MaxAuthTries` defaults to 6, so a well-stocked agent can exhaust the server before a password is reached. The provider warns rather than capping — silently dropping identities could discard the one key that would have worked, and the server's own "too many authentication failures" is easier to act on than a connection that fails with nothing reported.
- [x] 11.2 Full build (`build.ps1`) — package changes force a restore. **Done 2026-08-08**, green in 80.9s.
- [x] 11.3 Full test suite (`run-tests.ps1 -Headless`); zero failures, no `[Ignore]`. **Done 2026-08-08 — 6883/6883**, 138s, 0 crashes, no `[Ignore]` added.
- [x] 11.4 Manual smoke: PuTTY SSH2 with a stored password; PuTTY SSH2 with Pageant loaded; OpenSSH with a key; OpenSSH with a vault password (expect the new warning); SSH file transfer upload. **Done 2026-08-12 — all five pass.** Run against a real `sshd` (`linuxserver/openssh-server`, OpenSSH 10.3, port-mapped to `127.0.0.1:2299`) with PuTTY 0.84, Pageant 0.84 and `C:\Windows\System32\OpenSSH\ssh.exe` (OpenSSH_for_Windows_10.3p1). Each case was driven through the production code — `PuttyBase.Connect`, `ProtocolOpenSSH.BuildSshArguments`, `SecureTransfer` — from a throwaway NUnit harness, not from hand-written command lines; the harness was deleted afterwards because it depends on an externally provisioned container and on GUI processes.

  - **(a) PuTTY with a stored password — pass.** Line built: `-load "Default Settings" -ssh -2 -l tester -pwfile \\.\PIPE\mRemoteNGSecretPipe<rand> -P 2299 127.0.0.1`. Pageant was not running, no key was configured and discovery returned nothing, so `-pwfile` was the only credential and the login can only have come from it. The password never appeared on the command line.
  - **(b) PuTTY with Pageant loaded — pass.** Line built: `-load "Default Settings" -ssh -2 -l tester -P 2299 127.0.0.1` — no `-pw`, no `-i`. With nothing on the line to authenticate with, the completed login is the agent's. This is the case D5 leaves entirely to PuTTY: `ForPutty` sets `ConsultAgent: false`, and Pageant is reached natively.
  - **(c) OpenSSH with a key — pass.** Line built: `-p 2299 <SSHOptions> -i "<key>" tester@127.0.0.1`; `ssh.exe` ran a remote command and returned its output.
  - **(d) OpenSSH with a password — pass, with one caveat.** The 8.3 warning fired verbatim and the connection still succeeded on the key, which is the intended "report, do not drop silently, do not block" behaviour. **No live Vault/Delinea instance was available**, so the case ran with a stored password: identical code path, and only the provenance name inside the message differs (`this connection` instead of the provider). The provider-named wording is covered by `OpenSshArgsAdapterTests`.
  - **(e) SSH file transfer upload — pass.** See 9.6.

  **Observation worth keeping.** Every PuTTY connection stalls indefinitely if Pageant is running but wedged — PuTTY reports `Pageant is running. Requesting keys.` and then blocks, because the agent's message loop is busy with a modal dialog. It cost a while to diagnose here, and it is not specific to mRemoteNG: PuTTY has no timeout on the agent request. Relevant to 11.1's large-identity-set warning and to `add-ssh-agent-key-injection`, which will make mRemoteNG talk to Pageant itself.

  **Not covered by this smoke:** the reparenting of PuTTY's window into the tab and `ProtocolOpenSSH`'s `ConsoleControl` hosting (both need a message pump and neither is touched by this change), `SSHTransferWindow`'s own UI, and physical FIDO hardware (see 10.1).
- [x] 11.5 Confirm zero new analyzer warnings. **Done 2026-08-08.** Every warning in the full build predates this change. One that did not — RCS1075 (empty catch) introduced by section 5's `SshNetAgentProvider.Describe` — was fixed here rather than left.
- [x] 11.6 `openspec validate add-ssh-agent-credential-resolver --strict`. **Done 2026-08-08 — valid.**
